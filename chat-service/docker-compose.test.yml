services:
  db:
    image: postgres:18-alpine
    restart: always
    environment:
      POSTGRES_DB: db_chat_test
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  redis:
    image: redis:8-alpine
    restart: always
    ports:
      - "6379:6379"
